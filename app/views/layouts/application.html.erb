<!DOCTYPE html>
<html lang=<%= I18n.locale %>>
  <head>
    <title><%= (content_for(:title) ? "#{content_for(:title)} | " : '') + 'Frogogo' %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <meta name="description" content="Frogogo test shop">

    <%= vite_client_tag %>
    <%= vite_stylesheet_tag 'application' %>
    <%= vite_javascript_tag 'application' %>
  </head>

  <body>
    <%= render '/layouts/partials/notices' %>
    <header>
      <%= render '/layouts/partials/header' %>
    </header>
    <main>
      <div class="container mx-auto max-w-3xl sm:px-0 px-2">
        <%= yield %>
      </div>
    </main>
    <footer>
      <p class="my-10 text-gray-200 text-center text-sm leading-5">© 2021-<%= Time.current.year %> Eldar All rights reserved.</p>
    </footer>
  </body>
</html>
